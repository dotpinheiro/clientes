const styles = theme => ({
  titleContainer: {
    display: "flex",
    alignItems: "center",
    justifyContent: "center",
    margin: "2%"
  },
  mainTitle: {
    fontSize: "2em",
    textAlign: "center",
    width: "50%"
  }
});

export default styles;
