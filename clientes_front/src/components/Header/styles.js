import { makeStyles } from "@material-ui/core";

const styles = makeStyles({
  headerContainer: {
    backgroundColor: "#303f9f",
    display: "flex",
    alignItems: "flex-cend",
    justifyContent: "flex-end",
    position: "fixed",
    width: "100%",
    height: "60px",
    top: 0,
    left: 0
  },
  divider: {
    marginBottom: "8%"
  }
});

export default styles;
